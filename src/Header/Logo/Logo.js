import React from "react";
import styles from "../Logo/Logo.module.css";

const Logo = () => {
    return (
        <div className={styles.container}>
            <p className={styles.logo}>Social Page</p>
        </div>
    )
};

export default Logo;
